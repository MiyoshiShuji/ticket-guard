name: Infra Plan (What-if)

on:
  pull_request:
    paths:
      - 'infra/**.bicep'
      - 'infra/**.json'
      - '.github/workflows/infra-plan.yml'
  push:
    branches: [ main ]
    paths:
      - 'infra/**.bicep'
      - 'infra/**.json'
      - '.github/workflows/infra-plan.yml'

permissions:
  id-token: write
  contents: read

concurrency:
  group: infra-plan-${{ github.ref }}
  cancel-in-progress: false

env:
  LOCATION: japaneast
  RG_ONLY_TEMPLATE: infra/rg-only.bicep
  PARAMETERS_FILE: infra/parameters.poc.json

jobs:
  what-if:
    name: What-if (subscription scope)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Azure Login (OIDC)
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Azure CLI Version
        run: az version

      - name: What-if (RG only)
        run: |
          set -eux
          az deployment sub what-if \
            --name plan-${{ github.run_id }} \
            --location $LOCATION \
            --template-file $RG_ONLY_TEMPLATE \
            --parameters @$PARAMETERS_FILE \
            --parameters resourceGroupName=$(jq -r '.parameters.resourceGroupName.value' $PARAMETERS_FILE) location=$(jq -r '.parameters.location.value' $PARAMETERS_FILE)

      - name: Summary
        if: always()
        run: |
          echo "### Infra What-if Result" >> $GITHUB_STEP_SUMMARY
          echo "Template: $RG_ONLY_TEMPLATE" >> $GITHUB_STEP_SUMMARY
          echo "Mode: Resource Group creation only (Function/App resources deferred)" >> $GITHUB_STEP_SUMMARY
          echo "Parameters file: $PARAMETERS_FILE" >> $GITHUB_STEP_SUMMARY

